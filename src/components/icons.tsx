import type { LucideProps } from "lucide-react";
import { ShieldCheck } from "lucide-react";

export const Icons = {
  AppLogo: (props: LucideProps) => <ShieldCheck {...props} />,
};

export default Icons;
