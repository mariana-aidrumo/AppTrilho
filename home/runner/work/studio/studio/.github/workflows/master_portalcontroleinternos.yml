# Este workflow foi desativado e pode ser removido.
# O workflow ativo é o build-test.yml
name: Old Azure Deploy Workflow (Disabled)

on:
  push:
    branches:
      - "non-existent-branch" # Previne a execução automática
  workflow_dispatch:

jobs:
  do_nothing:
    runs-on: ubuntu-latest
    steps:
      - name: Log a message
        run: echo "Este workflow está desativado. Use o 'Build and Test Next.js App'."
