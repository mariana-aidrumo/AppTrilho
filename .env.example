# SharePoint Integration Credentials
# Copy this file to .env and fill in the values for local development.
# These variables also need to be set in your production environment (e.g., Vercel, Netlify).
SHAREPOINT_TENANT_ID=
SHAREPOINT_CLIENT_ID=
SHAREPOINT_CLIENT_SECRET=
SHAREPOINT_SITE_URL=

# Uncomment the line below to use mock data instead of connecting to SharePoint.
# USE_MOCK_DATA=true
